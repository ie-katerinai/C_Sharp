// Функция создает матрицу заполненную нулями

int[] имя_массива = new int[кол-во элементов] // тип_массива [пустой массив] имя_массива = новый_массив типа[кол-во элементов]. Если вводим кол-во элементов с клавиатуры, то тут пусто
for (int i = 0; int < имя_массива.Length; i++)
{
    Console.Write(имя_массива[i]+ " "); // заполняет в строку нулями через пробел. Если использовать WriteLine, то напечатает в столбик
}
